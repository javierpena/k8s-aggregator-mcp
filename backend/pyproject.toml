[project]
name = "demo-mcp"
version = "0.1.0"
description = "Demo MCP server providing /proc/cpuinfo information"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.0.0b2",
]
